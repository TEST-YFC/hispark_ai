# 算子工作流待办

本文件由 `scripts/workflow_state.py init` 在本轮开始时生成。它是状态文件的可读投影，
不要手工修改；每次阶段状态变化都会通过原子替换重新生成。

- 算子：`{{OPERATOR}}`
- 本轮 ID：`{{RUN_ID}}`
- 执行范围：`{{MODE}}`
- 固件 SDK：`{{SDK_ROOT}}`
- 状态文件：`{{STATE_FILE}}`
- 创建时间：`{{CREATED_AT}}`

## 任务

{{TASK_ROWS}}

任务行前的 `Stage0` 到 `Stage7` 是工作流唯一的阶段编号；`terminal.report` 是 Stage7 的收尾子步骤。任务 ID 与阶段编号一致，不能手工改名。

## 状态约定

- `[ ]` 待执行，`[~]` 执行中，`[x]` 通过
- `[!]` 失败或被失败阶段阻断，`[?]` 默认流程中尚未执行
- `[-]` 用户明确选择 Host-only，因此板端任务不在本轮范围

状态以同目录的 `workflow_state.json` 为准；读取或更新状态时，脚本也会核对本文件的任务行。
恢复任务时使用同一个本轮 ID，不能把旧轮次的日志、文档、固件或串口输出当作本轮证据。
