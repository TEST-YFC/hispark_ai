---
name: hs-verify-op-board
description: >-
  准备并验证 WS63 MindSpore Lite Micro 推理或 FP32 训练算子的真实板端用例。用于板端精度、板端训练验证、只做上板验证，或推理和训练工作流调用。复用 Host 已通过的模型、数据和参考结果，核对 SDK，准备模型库与 Sample，委托构建烧录并比较完整板端数据。不设计 Host 用例、不实现正式算子源码。
---

# 算子板端验证

## 选择验证类型

使用 `verification_kind=inference|training`，由调用方明确传入。独立请求包含训练、反向传播、梯度或权重更新时选择 `training`；没有训练语义的旧调用默认 `inference`。两种模式分别读取自己的步骤，不把推理结果当作训练结果。

- 推理：读取 [推理验证步骤](references/inference/workflow.md)。现有脚本、参数和结果格式保持不变。
- 训练：读取 [训练验证步骤](references/training/workflow.md)。只按本分支规定的训练模型、执行入口和数值证据作结论。

`framework_scope` 仍表示源模型框架，例如 `onnx`、`tflite`；它不是验证类型。`fp32` 等数值类型也单独记录。

## 共用规则

1. 开始前核对代码根、工具包、独立产物目录、运行环境和用户限定的操作范围。保留工作区已有修改。
2. 用例与结果绑定 `verification_kind`、`run_id`、`case_id`、框架、数值类型及模型/输入/参考结果指纹。不同轮次、不同验证类型不能混用结果。
3. 用例范围先确定再执行。失败不能通过删除用例、替换参考结果、缩小统计范围或临时放宽误差来消除。
4. `PASS` 表示本阶段预期检查全部执行且通过；真实执行错误为 `FAIL`；前置条件不足为 `NOT_RUN` 并记录原因、检测证据和重跑命令；用户明确排除才用 `NOT_REQUESTED`。不支持的能力记录具体限制，不能算通过。
5. 保留每条命令的退出码、完整日志和本轮实际产物。缺失文件、截断数据、旧结果和 Sample 自报成功都不能替代验证证据。
6. 正式算子源码缺陷交回相应实现技能；跨阶段问题交回调用工作流。专项请求停在当前阶段，不擅自扩大修改范围。

## 板端前置

SDK 写入前必须核对用户给出的固件 SDK 路径和芯片身份，不能把 skills 目录直接当作 SDK。复用 `scripts/probe_serial_ports.py` 保存端口探测结果；没有设备时记录 `NOT_RUN`，不能把 Host 执行说成上板通过。

固件构建、烧录分别调用 `hs-dev-build`、`hs-dev-flash`，串口采集使用已安装的串口技能或工作流指定工具。Host 已通过的预期用例全部保留；设备不足或资源限制导致未执行的行仍须出现在报告中。
