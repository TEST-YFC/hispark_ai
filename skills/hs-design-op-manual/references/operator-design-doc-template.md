# {Op} 算子设计文档

> 本文只记录算子规格、支持范围和软件设计，不记录测试设计、运行结果或板端验收结论。
>
> 文件位置：`<opdir>/docs/{op}-operator-design-doc.md`

## 1. 算子概述

### 1.1 功能和数学定义

{根据已核对规格说明功能；没有可靠公式时省略公式。}

### 1.2 最小示例

{给出输入 dtype、shape、属性和输出的最小示例。}

## 2. 框架算子规格

### 2.1 {ONNX/TFLite source entry}

| 属性 | 类型 | 默认值 | 是否必需 | 说明 |
|---|---|---|---|---|
| ... | ... | ... | ... | ... |

| 输入 | 模型数据类型 | 参数含义 | 规格限制 |
|---|---|---|---|
| ... | ... | ... | ... |

| 输出 | 模型数据类型 | 参数含义 | 规格限制 |
|---|---|---|---|
| ... | ... | ... | ... |

### 2.2 支持范围与限制

{只写已核对的 dtype、shape、layout、属性、方向和不支持范围。}

## 3. MindSpore Lite Micro 软件设计

### 3.1 七类能力与复用裁决

| 能力 | 复用/修改/新建/N/A | 主要文件或注册点 | 依据与限制 |
|---|---|---|---|
| Schema | ... | ... | ... |
| Parser | ... | ... | ... |
| Populate/Parameter | ... | ... | ... |
| Infer | ... | ... | ... |
| Kernel | ... | ... | ... |
| OpCoder | ... | ... | ... |
| Quantizer | ... | ... | ... |

### 3.2 关键场景与软件行为

| 使用场景 | 什么时候会遇到 | 软件行为与限制 |
|---|---|---|
| ... | ... | ... |

### 3.3 转换与运行调用链

```text
源模型节点 → Parser → Primitive/Schema → Populate/Parameter → Infer
→ 图优化/量化（如适用） → OpCoder → converter_lite 生成模型 C 代码
→ 静态库 → 应用模型 API → ExecuteN() → Kernel → 输出 Tensor
```

{必须替换为当前算子的真实入口、Primitive、参数、Kernel、OpCoder 和生成函数；不适用的环节写 N/A 及原因。}

> 七类能力不是每次推理按顺序执行：Parser、Populate、Infer、OpCoder 主要在转换或生成期使用，Kernel 在运行期执行。
